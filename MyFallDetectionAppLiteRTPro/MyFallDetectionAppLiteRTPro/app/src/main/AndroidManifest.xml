<!-- AndroidManifest.xml (Phone) -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Required permissions for the phone app -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_HEALTH" />
    <uses-permission android:name="android.permission.BODY_SENSORS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">

        <!-- Main (phone) activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Background service for fall detection -->
        <service
            android:name=".BackgroundFallService"
            android:exported="false"
            android:foregroundServiceType="health" />

        <!-- Wearable listener service to receive messages from the watch -->
        <service
            android:name=".wearintegration.PhoneWearListenerService"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.MESSAGE_RECEIVED" />
                <data android:scheme="wear" android:host="*" android:pathPrefix="/watch_accel_data" />
                <data android:scheme="wear" android:host="*" android:pathPrefix="/watch_status" />
                <data android:scheme="wear" android:host="*" android:pathPrefix="/watch_heartbeat" />
            </intent-filter>
        </service>

    </application>
</manifest>